name: GameCenter
options:
  bundleIdPrefix: com.eri
  deploymentTarget:
    iOS: "15.0"
  createIntermediateGroups: true
settings:
  base:
    SWIFT_VERSION: "5.0"
    TARGETED_DEVICE_FAMILY: "1,2"
    CODE_SIGN_STYLE: Automatic
targets:
  GameCenter:
    type: application
    platform: iOS
    deploymentTarget: "15.0"
    sources:
      - GameCenter
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.eri.gamecenter
        PRODUCT_NAME: GameCenter
        INFOPLIST_FILE: GameCenter/Info.plist
        GENERATE_INFOPLIST_FILE: NO
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        TARGETED_DEVICE_FAMILY: "1,2"
        SWIFT_VERSION: "5.0"
        CODE_SIGN_STYLE: Automatic